# elbencho append-only mode -- Orchestrator Handoff

I have a C++ storage benchmarking project called elbencho at:
/Users/eelliott/Library/CloudStorage/OneDrive-NetAppInc/Documents/Development/Work/elbencho

Repository: `breuner/elbencho` (GitHub, default branch: master)

**Current state -- T01/T02/T03 implemented and pushed. Read plan.yaml before doing anything.**

Plan: `docs/plan/append-only-mode-20260414/plan.yaml`

---

## Completed (all in one commit, pushed to feat/append-only-mode)

| Task | Description | Commit |
|------|-------------|--------|
| T01 | `--appendonly` flag in ProgArgs.h/.cpp, checkArgs guards (requires --timelimit, blocks read/verify/mmap/sharedfile), fileSize sentinel INT64_MAX/2 | 9340e8c |
| T02 | `OffsetGenUnbounded` class added to `source/toolkits/offsetgen/OffsetGenerator.h` -- huge numBytesLeft sentinel, advances currentOffset by blockSize per call | 9340e8c |
| T03 | LocalWorker.cpp/.h -- `getDirModeOpenFlags` returns `O_CREAT|O_WRONLY` (no O_APPEND), `dirModeOpenAndPrepFile` fstats file after open to seed `appendOnlyStartOffset`, skips ftruncate/fallocate, fileSize assertion bypassed for append-only runs | 9340e8c |

All changes are on `feat/append-only-mode`. plan.yaml `last_executed_task` = T03 (plan.yaml not updated yet -- still shows all tasks pending, update it as you go).

---

## Next batch -- please execute now

### T04 -- Integration review (gem-reviewer)

Review the five changed files for correctness against the pre-mortem checklist in plan.yaml:

1. Confirm `O_APPEND` is NOT used -- open flags are `O_CREAT | O_WRONLY` only
2. Confirm `fstat` is called after `open()` and `st.st_size` is used to seed `appendOnlyStartOffset`
3. Confirm `OffsetGenUnbounded` is initialized with `appendOnlyStartOffset` (not zero)
4. Confirm all three `checkArgs()` guards are present: missing `--timelimit`, read/verify conflict, sharedfile conflict
5. Confirm `fileSize` sentinel (`INT64_MAX/2`) does not leak into buffer-sizing or loop-bound code
6. Confirm `OffsetGenUnbounded::getNextOffset()` has overflow guard at the sentinel ceiling
7. Confirm non-appendonly code paths are unchanged (all new code is under `if (doAppendOnly)`)

Files to review:
- `source/ProgArgs.h`
- `source/ProgArgs.cpp`
- `source/toolkits/offsetgen/OffsetGenerator.h`
- `source/workers/LocalWorker.cpp`
- `source/workers/LocalWorker.h`

Flag blocking vs. non-blocking issues. Non-blocking issues do not delay T05.

### T05 -- Build and smoke test (gem-implementer, on Linux -- Ubuntu 22.04+)

**This task must run on a Linux box.** macOS cannot build this target.

**Install deps (Ubuntu):**
```
sudo apt -y install build-essential cmake debhelper devscripts fakeroot git libaio-dev \
  libboost-filesystem-dev libboost-program-options-dev libboost-thread-dev \
  libcurl4-openssl-dev libncurses-dev libnuma-dev lintian libssl-dev uuid-dev zlib1g-dev
```

**Clone branch:**
```
git clone https://github.com/breuner/elbencho.git
cd elbencho
git checkout feat/append-only-mode
cd external && bash prepare-external.sh && cd ..
```

**Build:**
```
make clean && make -j$(nproc) 2>&1 | tee /tmp/elbencho-build.log
```
Confirm: zero errors, zero new warnings from changed files.

**Smoke test -- append-only:**
```
mkdir -p /tmp/elbench-append-test
./bin/elbencho --appendonly --timelimit 5 -n 4 --block 1m /tmp/elbench-append-test
```
Confirm: exits cleanly after ~5s, reports non-zero throughput and total bytes, files exist and are non-empty.

**Smoke test -- regression (normal write):**
```
mkdir -p /tmp/elbench-normal-test
./bin/elbencho -w -s 64m -n 4 --block 1m /tmp/elbench-normal-test
```
Confirm: 4 x 64 MiB files, correct sizes.

**Error path test:**
```
./bin/elbencho --appendonly -n 1 /tmp/elbench-append-test
```
Confirm: non-zero exit code, message references `--timelimit`.

**Cleanup:**
```
rm -rf /tmp/elbench-append-test /tmp/elbench-normal-test
```

---

## Execution rules

- Branch: `feat/append-only-mode` -- all commits go here
- Do NOT merge to master without explicit user approval
- T05 requires a Linux host -- macOS will not compile (libaio, Linux syscall headers)
- The `external/prepare-external.sh` step is required once per fresh clone
- Tools: `make`, `g++` (7+), `git`; no special build flags needed for append-only mode
- If T04 finds blocking issues, delegate fixes to gem-implementer before running T05
- plan.yaml task statuses are stale (all show pending) -- update as each task completes
